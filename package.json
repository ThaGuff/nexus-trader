{
  "name": "nexus-trader",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "bot",
    "dashboard"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=bot\" \"npm run dev --workspace=dashboard\"",
    "build": "npm run build --workspace=dashboard",
    "start": "node bot/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.47.0"
  }
}
